# Web / simulateur iOS (sur la même machine que le backend) :
EXPO_PUBLIC_API_URL=http://127.0.0.1:8000

# Simulateur Android :
# EXPO_PUBLIC_API_URL=http://10.0.2.2:8000

# Téléphone physique (Expo Go) : IP LAN de TA machine (pas 127.0.0.1 !)
# Trouver ton IP : `ipconfig getifaddr en0` (Mac) ou `ipconfig` (Windows)
# EXPO_PUBLIC_API_URL=http://192.168.x.x:8000
